---
title: "UI-LIC: A Unified Framework for Evaluating Learned Image Compression Models"
title_zh: "UI-LIC: 一种用于评估学习图像压缩模型的统一框架"
authors: "Nicholas J. Nolen, Luc Trudeau, Andrew C. Freeman"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23545v1"
tags: ["query:xiqa"]
score: 8.0
evidence: "使用质量指标(PSNR, SSIM, VMAF, LPIPS)评估学习型图像压缩模型。"
tldr: 当前学习图像压缩（LIC）方法评估常因软件实现异构、训练超参差异及评价流程不统一而难以公平比较。为此，我们构建UI-LIC开源框架，纳入六个代表性LIC模型，通过中央控制器统一设定训练、推理和分析的共享参数。其图形用户界面支持在相同比特率下与传统视频帧内编码器进行对比，同时计算PSNR、SSIM、VMAF和LPIPS等多种质量指标，并配以交互式图像分析器和可配置的质量热图叠加。该框架仅需运行一条指令即可部署，为LIC研究提供便捷的客观对比与主观分析工具，以MIT许可证开放，有效降低了评估门槛并推动标准化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23545v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 665, \"height\": 352, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23545v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1744, \"height\": 818, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23545v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 1026, \"label\": \"Table\"}]"
motivation: LIC模型评估缺乏标准化流程，软件栈、训练条件和评测方法各异，导致复现与公平对比困难，急需统一平台。
method: 开发UI-LIC框架，集成六种LIC模型，提供集中控制器统一管理共享参数的训练、推理和分析，并配备GUI程序。
result: 框架实现与传统帧内编码器等比特率对齐，计算PSNR、SSIM、VMAF、LPIPS等客观指标，并提供可配置质量热图的交互式分析。
conclusion: 开源框架一键部署即可解锁多维度对比与主观分析，降低LIC研究门槛，以MIT许可证发布，有助于推动标准化评估。
---

## 摘要
学习图像压缩（LIC）系统的评估和比较因异构软件堆栈、不同的训练条件以及有分歧的评估方法而变得复杂。为了应对这些挑战，我们提出了UI-LIC，一个用于评估LIC模型的开源软件框架。我们集成了六个高性能LIC模型，并提供了一个集中控制器，使用共享配置参数执行训练、推理和分析。我们的GUI程序提供了一个精简的界面，以将这些模型与传统视频帧内编码器一起评估，均衡压缩比特率并计算质量指标，如PSNR、SSIM、VMAF和LPIPS。最后，我们提供了一个交互式图像分析器，带有可配置的质量热图叠加。我们的框架降低了进一步LIC研究的障碍，只需一条设置命令即可解锁比较指标和主观分析。该开源软件根据MIT许可证发布，并可在github.com/BaylorMultimediaLab/UI-LIC上获取。

## Abstract
The evaluation and comparison of Learned Image Compression (LIC) systems is complicated by heterogeneous software stacks, varying training conditions, and divergent evaluation methodologies. To address these challenges, we introduce UI-LIC, an open-source software framework for evaluating LIC models. We integrate six high-performance LIC models, and provide a centralized controller for performing training, inference, and analysis with shared configuration parameters. Our GUI program offers a streamlined interface to evaluate these models alongside traditional video intra-frame encoders, equalizing the compressed bitrates and calculating quality metrics such as PSNR, SSIM, VMAF, and LPIPS. Finally, we provide an interactive image analyzer with configurable quality heatmap overlays. Our framework lowers barriers to further LIC research, unlocking comparative metrics and subjective analysis with a single setup command. The open-source software is released under the MIT license and is available at github.com/BaylorMultimediaLab/UI-LIC.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：学习图像压缩（LIC）领域发展迅速，但模型间的性能比较困难重重。不同工作通常采用差异化的软件栈、训练数据集、优化目标和评价指标，导致复现结果难、横向对比不可靠。
- **核心问题**：缺乏一套统一的、可复现的评估框架，使研究人员能够将多种LIC模型置于相同条件下进行训练、推理和客观/主观分析。尤其对于生成式压缩模型产生的“幻觉”失真，单一的数值指标难以反映真实质量，需要结合可视化工具。
- **整体含义**：论文旨在提供一个开源、一站式的统一界面（UI-LIC），通过管理共享配置、集成训练与评估管线、提供交互式可视化，解决了LIC评估碎片化问题，降低了研究门槛，推动社区走向标准化对比。

## 2. 论文提出的方法论

### 核心思想
构建一个模块化、解耦的软件框架，将模型训练、推理、指标计算和可视化分离，通过中央控制器和JSON配置统一调度多个LIC模型及传统编解码器。

### 关键技术细节与流程
- **环境隔离**：一键脚本为每个LIC模型建立独立的虚拟环境（Conda/venv），保留原作者指定的包版本，避免依赖冲突。同时为指标计算（含VMAF的Docker容器）建立统一环境。
- **统一接口体系**：
    - **基类解析接口**：提供标准化的参数解析方法，将模型特定参数映射为全局参数（如训练轮数、学习率、数据集路径），并校验必须参数。
    - **训练与测试接口继承**：每个LIC模型继承自集中式基类，通过别名定义实现模型间一致的训练/推理指令生成。
- **作业调度**：用户定义JSON配置，指定模型、数据集、参数以及是否启用比特率均衡。调度器加载接口和参数后，按需执行训练或推理作业。
- **模型适配**：对集成的六个LIC模型（ELIC、TCM、HPCM、DCVC-RT intra、StableCodec、RwkvCompress）进行了必要修改，如开发专用训练/推理脚本、统一数据加载方式（如ImageFolder）、标准化Bitstream保存等。
- **评估管线**：
    - **客观指标**：PSNR（含YUV分量及加权）、SSIM、VMAF、LPIPS（同时保存LPIPS AlexNet五层特征图，用于可视化）。
    - **错误图**：生成MSE、块级SSIM、归一化图像梯度的空间误差图。
    - **比特率均衡**：对于速率不可调的LIC，先获取其最小码流，然后以该码流为目标，通过QP搜索驱动传统编解码器（x264、x265、SVTAV1等）编码，实现等比特率对比。
- **交互式GUI（Tkinter）**：提供图形界面用于启动编解码、选择对比图像，展示并排比较（滑动蒙版）、多色LPIPS特征层叠加热图，以及全局指标报告。

## 3. 实验设计

- **数据集**：采用经典的**Kodak True Color**图像集（未提及划分方式，但从表1看是全量推理）。
- **Benchmark设置**：以**等比特率**作为比较基准（通过上文比特率均衡方法实现），对比LIC模型与传统帧内编码器的压缩质量。
- **对比方法**：
    - LIC模型：HPCM、TCM、RwkvCompress、StableCodec、DCVC-RT、ELIC。
    - 传统编解码器：AV1（SVTAV1 & NVENC）、AVC/H.264（x264 & NVENC）、HEVC/H.265（x265 & NVENC）。
- **评估指标**：bpp（码率）、PSNR、SSIM、LPIPS、VMAF。

## 4. 资源与算力

- 论文**未明确说明**训练或推理所消耗的GPU型号、数量及训练时长。仅提到框架支持训练和推理，且集成的模型使用官方预训练权重进行演示。推测其主要计算负载来自模型推理和传统编解码器QP搜索，但具体算力需求未被记录。

## 5. 实验数量与充分性

- **实验数量**：论文仅展示了一组基于Kodak数据集的整体性能对比（表1），涉及6个LIC模型和对应的传统编解码器在多个等比特率点上的结果，以及一幅生成式压缩幻觉示例（图1）和一对可视化对比截图（图2）。
- **充分性评估**：
    - **观点1（框架演示充分）**：作为软件框架论文，其目标并非验证模型性能，而是展示框架的可用性与对比统一性。表1证明了框架能够成功执行多模型等比特率评估、输出客观指标，且可比较传统编码器，功能验证基本充分。
    - **观点2（实验结论深度不足）**：缺乏多数据集覆盖（如CLIC、Tecnick等）、消融研究（如不同训练配置对结果的影响）、统计显著性检验，难以得出关于各个模型性能优缺点的科学结论。但综合来看，对于以“框架”为主的论文，其实验设计客观、公平（共享环境、等比特率），已较好地完成了工具性的演示。

## 6. 论文的主要结论与发现

- UI-LIC框架成功实现了多个异构LIC模型在统一训练、推理和评估管线下的可复现对比。
- 通过比特率均衡，可将传统编解码器纳入同一尺度，直观展示学习型与手工设计编码器的性能差异（例如，DCVC-RT在当前配置下全面优于传统编码器）。
- GUI提供的LPIPS特征层叠加等可视化工具，能有效揭示生成式压缩模型（如StableCodec）在语义层面的重建问题（如天空区域表现好但物体细节丢失），弥补了单一数值指标的不足。
- 整体上，该框架将LIC研究的工程准备时间（环境配置、模型集成）大幅压缩至“一条命令”，提升研究透明度和可访问性。

## 7. 优点

- **统一性与可复现性**：为不同来源的LIC模型提供一致的训练与评估环境，消除碎片化，是领域内亟需的基础设施。
- **兼容性设计**：采用虚拟环境隔离，不强制统一依赖，尊重原始实现，降低集成障碍；同时支持传统编解码器对比，拓宽应用场景。
- **丰富的分析工具**：不仅提供多维度客观指标，还创新性利用LPIPS中间层特征图生成彩色叠加热图，使“哪里失真、失真类型”一目了然，尤其适用于诊断生成式压缩的幻觉问题。
- **用户友好**：提供JSON配置驱动、命令行接口和GUI，兼顾自动化与交互式探索，且基于MIT开源协议，便于社区扩展。
- **实用的比特率均衡**：针对速率不可调LIC模型的对比需求，设计了自动化QP搜索流程，实现公平的等码率比较。

## 8. 不足与局限

- **模型覆盖度有限**：目前仅集成6个LIC模型，后续需持续纳入更多前沿方法，以维持框架的通用性和权威性。
- **实验验证的深度**：仅用Kodak一个标准数据集作为效能展示，未进行跨泛化性测试；也未提供训练部分的稳定性、收敛速度等基准报告，对“训练”组件的验证相对薄弱。
- **算力与扩展性未讨论**：缺失对大规模训练时所需硬件资源的建议，如何支持分布式训练也未提及，可能限制其在算力敏感研究中的应用。
- **GUI及分析功能局限**：侧面的并排对比虽直观，但缺少批量统计、显著性分析、主观评分接口等进阶功能；可视化仅支持图像，尚未覆盖视频。
- **对生成式压缩的评价维度**：虽然LPIPS特征层可视化很有价值，但并未系统性地定义或集成评估“语义保真度”或“人脸复原度”的专用度量，对这类模型的诊断能力仍可加强。

（完）
