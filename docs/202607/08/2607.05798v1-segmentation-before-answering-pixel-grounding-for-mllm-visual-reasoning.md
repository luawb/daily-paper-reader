---
title: "Segmentation before Answering: Pixel Grounding for MLLM Visual Reasoning"
title_zh: 先分割再回答：面向多模态大模型视觉推理的像素级定位
authors: "Yake Wei, Yuan Wang, Fengyun Rao, Jing Lyu, Di Hu"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05798v1"
tags: ["query:xiqa"]
score: 8.0
evidence: 为MLLM视觉推理提出基于分割的像素级定位
tldr: 多模态大语言模型（MLLM）的视觉推理往往需要缩放感兴趣区域以获取细节，但传统边界框常引入无关背景和干扰对象。本文提出SegAnswer方法，在回答前对区域进行像素级分割，以精确掩码替代边界框，有效过滤冗余信息，使视觉输入更契合模型处理方式。实验证明，该方法在高分辨率感知、通用视觉和幻觉等多项基准上均带来一致提升，并在分割任务中表现优异，为可靠像素级定位推理提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05798v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1703, \"height\": 810, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 1341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1700, \"height\": 1421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05798v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1729, \"height\": 376, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05798v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 817, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05798v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1865, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05798v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1696, \"height\": 481, \"label\": \"Table\"}]"
motivation: 现有MLLM在视觉推理时常使用边界框缩放感兴趣区域，但边界框内常包含无关背景和干扰物，影响模型获取精细细节。
method: 提出SegAnswer，在回答前使用像素级分割掩码替代边界框，精准隔离目标区域并过滤背景噪声，使视觉输入与MLLM的视觉标记方式更契合。
result: 在多个基准（包括高分辨率感知、通用感知和幻觉测试）上，SegAnswer取得一致性能提升，并在分割任务上表现突出，验证了像素定位的可靠性。
conclusion: 以分割掩码为缩放单元可有效滤除背景噪声，为MLLM提供更纯净的感兴趣区域，从而提升视觉推理的准确性和鲁棒性。
---

## 摘要
近年来，多模态大语言模型已从静态感知演进到交错的视觉-语言推理，常被称为“用图像思考”。这一推理过程的基本操作之一是放大感兴趣区域（通常用边界框表示）以获取更精细的视觉细节。本文提出“先分割再回答”（SegAnswer），将放大操作的基本单元从流行的边界框转变为像素级分割掩码。通过采用细粒度掩码将目标区域从杂乱环境中分离出来，分割后的视觉输入能提供更精确的感兴趣区域，有效滤除冗余背景和干扰物体。此外，分割视觉输入的离散图像块与多模态大语言模型通过位置嵌入构建视觉标记的方式更加无缝对齐。在实验中，我们在多个基准上评估SegAnswer，包括高分辨率感知、通用感知和幻觉测试。它实现了持续提升，并在分割任务上表现出可观的性能，验证了其可靠的像素级定位能力。

## Abstract
Recent advancements in Multimodal Large Language Models (MLLMs) have evolved from static perception to interleaved visual-language reasoning, often referred to as ``thinking with images''. A basic operation in this reasoning process is to zoom in on regions of interest (often represented with bounding boxes) to acquire finer visual details. In this paper, we propose \textbf{Seg}mentation before \textbf{Answer}ing (SegAnswer), which shifts the unit of zoom-in from the popular bounding box to pixel-level segmentation mask. By employing fine-grained masks to isolate the target area from cluttered environments, segmented visual input yields a more precise region of interest, effectively filtering out redundant background and interfering objects. Furthermore, the discrete patches of segmented visual input align more seamlessly with how MLLMs structure visual tokens via positional embeddings. In experiments, we evaluate SegAnswer across diverse benchmarks, including high-resolution perception, general perception, and hallucination. It achieves consistent improvements and also exhibits considerable performance on segmentation tasks, validating its capability for reliable pixel grounding.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **核心动机**：多模态大语言模型（MLLM）在进行视觉推理时，常需“放大”感兴趣区域以获取细节，目前主流做法是生成矩形边界框（BBox）并裁剪。然而，边界框的刚性矩形无法匹配自然物体不规则形状，会引入大量冗余背景，并在物体重叠时无法分离目标，导致视觉干扰和语义模糊。
- **整体含义**：作者提出将缩放操作的基本单元从边界框转变为像素级分割掩码，通过精细的语义分割准确隔离感兴趣区域，从而消除背景噪声、解耦重叠实体，并为后续推理提供更纯净的视觉上下文。

## 2. 论文提出的方法论
- **核心思想**：在回答前执行分割（Segmentation before Answering），让模型能够输出精确的像素级掩码作为中间步骤，取代基于边界框的粗略缩放。
- **三阶段训练流程**：
  - **Stage 1：像素定位（Pixel Grounding）**
    - 基座模型：Qwen2.5‑VL‑7B。
    - 集成一个简单的 MLP 投影器和 SAM 2.1 作为掩码解码器，通过特殊 token `<|seg|>` 的隐藏状态驱动分割。
    - 训练时结合语言建模损失和分割特定损失（focal loss、dice loss、IoU 预测的 MAE loss、目标预测的交叉熵损失）。
    - 两阶段优化：先冻结 MLLM 主干和掩码解码器，只训练投影器；然后对 MLLM 施加 LoRA，联合微调投影器、掩码解码器和 LoRA 层。完成后合并 LoRA 权重。
  - **Stage 2：多模态交错监督微调（Multimodal Interleaved SFT）**
    - 教导模型在对话中判断是否需要进一步观察，如需要则生成 `<|seg|>` 触发分割，随后将分割后的图像（保留原图位置索引）输入模型，结合原图进行最终回答。
    - 仅用语言建模损失优化，冻结投影器和掩码解码器，全参数微调 MLLM。
  - **Stage 3：基于像素定位的推理（Reasoning with Pixel Grounding）**
    - 使用强化学习优化推理策略，状态为文本 tokens 和视觉 tokens（原图 + 分割图）的交错序列，视觉 tokens 保留原图位置索引。
    - 奖励信号为加权组合：准确率奖励 \(R_{acc}\) 和格式奖励 \(R_{format}\)（权重 0.8 和 0.2）。
    - 采用 DAPO 算法，仅对模型预测的 tokens 计算损失。
- **位置索引保留**：分割后的离散图像块保持其在原图中的位置嵌入，使稀疏的掩码区域仍能准确反映空间关系，与 MLLM 的视觉标记组织方式天然一致。

## 3. 实验设计
- **评估基准**：
  - **高分辨率感知**：V*、HR‑Bench 4K、HR‑Bench 8K。
  - **通用感知**：MMBench、VisuLogic、MMVP。
  - **幻觉评估**：POPE、Hallusionbench。
  - **分割能力**：RefCOCO、RefCOCO+、RefCOCOg。
- **对比方法**：
  - 通用 MLLM：LLaVA‑OneVision‑9B、Qwen2.5‑VL‑7B（基座）。
  - 基于 BBox 视觉推理的模型：Pixel Reasoner、DeepEyes。
  - 专门针对细粒度视觉搜索的方法：SEAL、DyFo、Chain‑of‑Focus。
  - 分割专用模型：LISA、Groundhog、LaSagnA、VideoLISA、VISA、Vitron。
- **训练数据**：Stage 1 使用 RefCOCO/+/g、RefClef、ReasonSeg、ADE20K 等多个分割数据集；Stage 2 使用 VisualCOT（438k 问答对，仅用答案监督，不使用边界框监督）；Stage 3 使用 ViRL39K（39k 可验证问答对）。

## 4. 资源与算力
- 文中**未明确说明** GPU 型号、数量或训练总时长。仅给出了不同阶段的学习率、epoch 等超参数（例如 Stage 1 投影器学习率 1e‑3，LoRA 训练 2e‑5；Stage 2 学习率 2e‑6；Stage 3 学习率 2e‑5）。

## 5. 实验数量与充分性
- **实验组数**：覆盖 3 大类感知基准（约 7 个数据集），1 类分割基准（3 个子集），共计 10 余个测试项；同时提供了与多个强基线和专用方法的对比表格（表 1、表 2、表 3）、定性推理案例（图 3）以及强化学习训练奖励曲线（图 4）。
- **充分性评估**：对比实验较为全面，涉及高分辨率、通用感知和幻觉等多个维度，且与基于边界框的推理方法进行了直接比较。但**缺少消融实验**（例如去除分割、替换为边界框、改变掩码精度等），因此无法完全验证各设计组件的独立贡献。实验整体公平，基准测试均使用统一的官方评估代码。

## 6. 论文的主要结论与发现
- SegAnswer 在所有高分辨率、通用感知和幻觉基准上均取得一致且可观的提升，尤其在需要精细视觉搜索的任务（如 V* 基准提升 8.9 个点）上优势显著。
- 相比基于边界框的视觉推理方法，像素级分割能更精准地隔离目标区域，有效滤除背景噪声，并减少语义干扰。
- 模型具备出色的像素级定位能力，在 RefCOCO 系列基准上超越多个分割专用方法，证明了其推理过程中分割结果的可靠性。
- 保留原图位置索引的分割图像块能够自然保留空间关系，且与 MLLM 视觉标记方式无缝对齐。

## 7. 优点
- **方法创新**：首次将视觉推理中的“感兴趣区域缩放”操作从边界框提升到像素级分割掩码，更精细、更灵活。
- **三阶段训练设计**：逐步赋予模型分割能力、交错对话能力和强化推理能力，且 Stage 2 有效缓解了 RL 冷启动问题。
- **位置索引保留策略**：充分利用分割后稀疏图像块的空间信息，与 Transformer 位置嵌入天然适配。
- **多维度验证**：同时评估推理质量和分割精度，且覆盖高分辨率、通用感知、幻觉等多种场景，结论稳健。

## 8. 不足与局限
- **缺乏消融分析**：未对关键组件（如分割精度的影响、是否替换回边界框、是否保留位置索引、各阶段训练的必要性等）进行消融实验，难以量化各设计的贡献。
- **计算资源未透露**：未给出 GPU 需求、训练时间等实用信息，复现成本未知。
- **模型规模限制**：仅基于 Qwen2.5‑VL‑7B 验证，未探索更大参数模型或不同架构的泛化性。
- **训练数据依赖**：Stage 2 和 Stage 3 依赖特定的指令/强化数据集，数据多样性可能影响应用效果；且 Stage 2 虽然未用边界框监督，但其数据本身基于边界框标记区域。
- **应用场景局限**：方法针对需要“放大观察”的推理设置，对于不需要精细区域检查的任务可能没有明显优势，且分割生成本身引入额外计算开销。

（完）
