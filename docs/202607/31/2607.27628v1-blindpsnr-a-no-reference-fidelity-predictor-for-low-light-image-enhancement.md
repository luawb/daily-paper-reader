---
title: "BlindPSNR: A No-Reference Fidelity Predictor for Low-Light Image Enhancement"
title_zh: BlindPSNR：一种用于低光图像增强的无参考保真度预测器
authors: "Mingzhe Lyu, Jinqiang Cui, Hong Zhang"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.27628v1"
tags: ["query:xiqa"]
score: 8.0
evidence: 低光图像增强的无参考图像质量评价
tldr: "低光图像增强（LLIE）方法的可调参数通常固定，导致跨场景性能下降，而手动选择最优参数耗时且不总可行。虽然峰值信噪比（PSNR）是自然的保真度准则，但需要不可得的参考图像；现有无参考图像质量评估（NR-IQA）方法关注感知质量而非信号保真度，在基准测试上参数选择准确率均为0%。本文利用成对训练数据中PSNR可解析计算的特点，提出BlindPSNR——一个轻量级无参考网络，通过窗口交叉注意力融合增强图像与退化低光输入，并以异方差回归估计PSNR。标量回归基线仅达54.4%的Top-1准确率，BlindPSNR将其提升至89.5%，平均遗憾从1.62 dB降至0.026 dB，且能泛化到未见数据集（SRCC 0.61-0.67），为低光增强自动参数优化提供首个可靠的无参考保真度预测器。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27628v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 446, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27628v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1878, \"height\": 834, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27628v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 925, \"height\": 426, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27628v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 750, \"height\": 226, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27628v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 676, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27628v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 751, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27628v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 634, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27628v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 636, \"height\": 252, \"label\": \"Table\"}]"
motivation: 低光增强中，最优参数选择依赖PSNR但缺乏参考图像，现有无参考质量指标因专注感知而非保真度导致精度为零。
method: 利用成对数据解析PSNR直接监督，设计轻量网络，以窗口交叉注意力融合增强与输入图像，通过异方差回归输出PSNR估计。
result: "BlindPSNR将盲选最佳参数准确率从54.4%提升至89.5%，遗憾降至0.026dB，跨数据集SRCC达0.61-0.67。"
conclusion: 提出首个面向低光增强的无参考保真度预测器，实现高效自动参数选择，显著优于现有感知质量指标，并具有良好的泛化能力。
---

## 摘要
低光图像增强（LLIE）方法涉及可调参数，这些参数通常是固定的，跨场景应用时往往导致性能下降。然而，手动选择最佳配置可能耗时且不总是可行。峰值信噪比（PSNR）是自动化参数选择的自然保真度准则，但它需要通常无法获得的地面真值参考。据我们所知，没有基于学习的方法解决低光图像增强的无参考PSNR预测问题；自然的替代方案，无参考图像质量评价（NR-IQA），针对的是感知质量而非信号保真度，并且我们测试的所有七个基线方法在我们的基准测试中均实现了0%的Top-1选择准确率。借助成对训练数据，地面真值PSNR是可解析计算的，从而提供了精确的监督，无需单独的教师网络。基于此，我们提出BlindPSNR，一种轻量级的无参考网络，通过窗口交叉注意力将增强图像与退化的低光输入融合，并通过异方差回归估计PSNR。标量回归基线达到了54.4%的Top-1准确率，而BlindPSNR将其提升至89.5%，遗憾值从1.62 dB降至0.026 dB，并泛化到未见数据集（SRCC = 0.61-0.67）。

## Abstract
Low-light image enhancement (LLIE) methods involve tunable parameters that are typically fixed, often leading to performance degradation when applied across scenes. Manually selecting the best configuration, however, can be time-consuming and not always practical. Peak signal-to-noise ratio (PSNR) is the natural fidelity criterion for automating parameter selection, yet it requires a ground-truth reference that is typically unavailable. To our knowledge, no learning-based method addresses no-reference PSNR prediction for low-light image enhancement; the natural surrogate, no-reference image quality assessment (NR-IQA), targets perceptual quality rather than signal fidelity, and all seven baselines we test achieve 0% top-1 selection accuracy on our benchmark. With paired training data, the ground-truth PSNR is analytically computable, providing exact supervision without a separate teacher network. Building on this, we propose BlindPSNR, a lightweight no-reference network that fuses the enhanced image with the degraded low-light input via windowed cross-attention and estimates PSNR through heteroscedastic regression. While a scalar-regression baseline achieves top-1 accuracy of 54.4%, BlindPSNR raises this to 89.5% with regret dropping from 1.62 dB to 0.026 dB, and generalizes to unseen datasets (SRCC = 0.61-0.67).

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：低光图像增强（LLIE）方法通常依赖固定参数配置，但在不同场景下固定参数易导致过增强、色彩失真或噪声放大，手动选择最佳参数耗时且不实用。
- **关键矛盾**：峰值信噪比（PSNR）是衡量信号保真度的自然指标，可用于自动选择最优增强候选，但它需要地面真值参考图像，而在实际部署中参考图像通常不可得。
- **现有解决方案的缺陷**：无参考图像质量评价（NR-IQA）方法预测的是感知质量而非信号保真度。论文通过实验表明，7种代表性的NR-IQA方法（包括传统和深度学习法）在增强候选选择任务上Top-1准确率均为0%，平均遗憾超过5 dB，且部分方法与PSNR呈负相关，系统性地偏好低保真度的输出。
- **研究动机**：填补“无参考PSNR预测”在低光图像增强领域的空白，利用成对训练数据中可解析计算的PSNR作为精确监督信号，训练一个无需参考图像即可估计PSNR的网络，以实现自动化候选选择。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：以增强图像和原始低光输入作为双输入，通过融合两种信息来推断增强过程中引入的噪声和失真，从而预测增强图像相对于未知地面真值的PSNR。
- **训练目标**：利用可解析计算的全局对数均方误差（log-MSE）作为监督目标，无需额外的教师网络。PSNR目标通过公式 \( s = \log(\text{MSE}(E, G) + \epsilon) \) 计算，其中 \(\epsilon = 10^{-6}\)，PSNR可从 \( s \) 恢复为 \(-4.343 s\)。
- **网络结构（BlindPSNR）**：
  - **共享骨干**：使用ConvNeXt-Tiny作为共享特征提取器，分别从增强图像 \(E\) 和低光图像 \(L\) 中提取多尺度特征（步长8、16、32）。
  - **窗口交叉注意力（Windowed Cross-Attention）**：在三个尺度上对 \(E\) 和 \(L\) 的特征进行交叉注意力融合。注意力限制在非重叠窗口中，窗口大小随尺度递减（8、4、2），以保留局部空间对齐信息。融合时拼接直接特征、绝对差值、逐元素乘积等四种互补线索。
  - **解码与预测**：通过FPN（特征金字塔网络）解码，对最高和最低分辨率特征图进行全局平均池化，得到256维向量，输入全局log-MSE预测头 \(s_S\)。同时，瓶颈处附加置信度头（对数方差）用于异方差回归。
- **损失函数**：双项损失联合训练。
  - 异方差回归损失：\( \mathcal{L}_{\text{reg}} = \frac{1}{2} e^{-v_S}(s_S - s)^2 + \frac{1}{2} v_S \)，通过学习方差对不确定样本进行降权。
  - 全局蒸馏损失：在相同目标上使用smooth-ℓ1损失 \( \mathcal{L}_{\text{gdist}} = \text{smooth-}\ell_1(s_S, s) \)，以均匀权重提供鲁棒梯度信号，系数 \(\lambda = 0.5\)。
- **推理阶段**：仅使用无参考网络，对每个候选增强图像预测PSNR \(\hat{p}_i = -4.343 s_S(L, E_i)\)，选择最高分的候选作为最优结果。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法

- **训练数据集**：多源训练集共2,423张图像、145,788个增强候选。包括：
  - 多视图校准数据集RealX3D（5个场景）和LOM（4个场景），使用5种增强方法（HVI-CIDNet、CIDNet-SICE、CLAHE、Gamma+Denoise、MSR），每图约230个候选。
  - LOL-v1、LOL-v2-real、LOL-v2-syn（共2,074张图像），使用3种经典增强方法，每图约34个候选。
  - mip-NeRF-360室内场景（40张图像），色调曲线候选，每图约90个候选。
- **评估数据集与场景（表一）**：
  - RealX3D留出集（57个场景，13,281个候选）：场景未见，用于主实验。
  - LOL-v2-real测试集（100组，3,500候选）：分割未见（同数据集不同划分）。
  - LSRW（50组，1,750候选）：领域未见（完全未参与训练）。
  - mip-NeRF-360留出集（16组，1,600候选）：场景未见。
- **对比方法**：
  - 传统NR-IQA：NIQE、BRISQUE、PIQE（得分越低越好，候选选择时反转符号）。
  - 学习型NR-IQA：MUSIQ、MANIQA、HyperIQA、TOPIQ-NR。
  - 自身基线：标量回归（无低光输入和全局蒸馏损失）及一系列消融变体。
- **评估指标**：Top-k准确率（所选候选PSNR与神谕候选之差≤0.1dB的比例）、遗憾值（所选候选与神谕候选的PSNR差距）、SRCC（斯皮尔曼等级相关系数，按场景组计算均值）。

### 4. 资源与算力

- 论文中明确说明：
  - 骨干网络：ConvNeXt-Tiny（ImageNet预训练，参数34.9M）。
  - 优化器：AdamW（\(\beta_1=0.9, \beta_2=0.999\)，权重衰减0.05），余弦学习率（初始3×10⁻⁴，带3个epoch预热）。
  - 批大小：16（2个场景 × 8个候选），图像裁剪尺寸384×384，训练30个epoch。
  - 推理速度：对233个候选评分耗时约4秒，使用单张RTX 4090 GPU。
- 未提及训练总时长或GPU总数，仅提及推理用RTX 4090。

### 5. 实验数量与充分性

- **主实验**：在留出的RealX3D（57组）上与7种NR-IQA基线及自身标量回归基线对比，报告了SRCC、Top-1、Top-5、遗憾值。
- **消融实验**：通过四项留一法消融（移除低光输入、移除全局蒸馏损失、移除两者）在相同留出集上评估各组件的贡献。
- **跨数据集泛化实验**：在三个未见数据集（LOL-v2-real、LSRW、mip-NeRF-360）上测试。
- **方法内排序实验**：对单一增强家族内的参数变体进行选择，验证模型对细微质量差异的敏感性。
- **数值稳定性**：实验覆盖了多个增强方法家族、不同数据分布，消融设计剥离了关键组件的独立效果，客观性较好。但部分实验结果仅在一个留出集上详细报告，跨数据集评估样本量相对较小（如mip-NeRF-360仅16组），整体实验数量可视为较充分，但泛化性验证的深度仍有提升空间。

### 6. 论文的主要结论与发现

- **现有NR-IQA与PSNR严重不对齐**：所有7个基线在候选选择任务上Top-1准确率为0%，甚至出现负相关，证明感知质量指标无法替代信号保真度指标。
- **BlindPSNR实现近神谕选择**：在留出集上获得89.5%的Top-1准确率、100%的Top-5准确率，平均遗憾仅0.026 dB，远优于标量回归基线（54.4% Top-1，1.62 dB遗憾）。
- **低光输入是最大单因子贡献，蒸馏损失是关键补充**：移除低光交叉注意力输入使Top-1下降29.9个百分点；移除smooth-ℓ1蒸馏损失降低10.6个百分点。两者结合才能达到最优选择质量。
- **跨域泛化存在但有限**：在未见数据集上的SRCC为0.61-0.67，Top-1准确率12%-38%，与留出集存在较大差距，表明训练数据的多样性和骨干网络的泛化先验仍有提升空间。

### 7. 优点：方法或实验设计上有哪些亮点

- **问题定义清晰且实用**：将低光增强的参数自动选择转化为无参考PSNR预测，直接瞄准信号保真度，相较于感知质量指标更贴合下游任务需求。
- **精确且无级的监督信号设计**：利用成对数据中直接计算的对数MSE作为目标，无需额外训练教师模型，实现端到端精确回归。
- **双输入融合机制有效**：窗口交叉注意力显式地利用低光输入作为增强前的参考上下文，帮助区分场景细节与增强伪影，大幅提升排序精度。
- **损失函数设计互补**：异方差回归结合smooth-ℓ1蒸馏形成“降噪+均衡”的监督策略，兼顾不确定性和鲁棒性。
- **全面的实验验证**：消融实验清晰归因各模块贡献，跨数据集和跨方法族的评估检验了泛化能力，定性与定量（如候选选择散点图）分析细致。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **跨域性能下降明显**：留出集上SRCC为0.96，但跨数据集降至0.61-0.67，Top-1准确率从89.5%陡降，说明模型对训练覆盖的增强方法类型和场景分布较敏感，泛化需更多样化的训练数据。
- **训练数据偏差风险**：训练集以多视图校准场景为主（如RealX3D、LOM），可能引入特定的增强伪影模式，对真实世界单图应用的支持可能不足。
- **方法支持的增强类型有限**：实验仅覆盖少数几类增强方法，未知对其他最新LLIE算法的适配性。
- **PSNR指标本身的局限性**：PSNR虽为保真度度量，但与人类视觉感知并不完全一致。该方法旨在自动化保真度选择，但未探讨保真度与感知质量的权衡场景。
- **算力与效率细节不完整**：未报告完整训练时长及所需GPU资源，仅给出推理速度，难以复现训练成本。
- **评价维度单一**：主要采用排序指标（Top-k、遗憾），未全面评估绝对PSNR预测精度（虽然文中指出排序更重要），在需要精确值输出的场景可能受限。

（完）
